{
  "private": true,
  "workspaces": [
    "packages/*",
    "microservices/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/shared"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.1"
  }
}