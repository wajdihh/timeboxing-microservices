# Node modules (applies to all services)
node_modules/

# Build artifacts
dist/

# Environment files (avoid leaking secrets)
.env.dev
.env.staging
.env.prod

# Logs
logs/
*.log

# Dependency lockfiles (optional for consistency)
package-lock.json
yarn.lock
dist/
